# Admin with login and registration
just run the application using " npm init " command
and then " npm start " 
then use when you stepup the package.json file.
run " npm run start " or  "npm run dev " 